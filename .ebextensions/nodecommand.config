option_settings:
  aws:elasticbeanstalk:container:nodejs:
    # Commenting out NodeCommand since we have a Procfile
    # NodeCommand: npm start
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx 